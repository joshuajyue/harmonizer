"""One-off experiments that produce numbers for the report.

Nothing in this package is imported by an engine, by the backend, or by the eval
harness. It exists so that claims in `ml/eval/REPORT.md` are reproducible rather
than asserted — which is the specific failure this project is a response to.
"""
